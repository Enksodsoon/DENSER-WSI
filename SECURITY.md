# Security Policy

Report vulnerabilities through GitHub private vulnerability reporting. Do not
open a public issue containing credentials, source-linked pathology metadata, or
local paths. The decoder must treat every container, index, certificate, repair,
and native-codec input as untrusted and fail closed.
